APP_HOST=
APP_PORT=

DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASS=

# only for a email confirmation feature
EXTERNAL_HOST=
SMTP_RELAY=
SMTP_LOGIN=
SMTP_PASS=

# only for local development
VITE_PORT=
ENV=test

# to call a remote api
CLIENT_API_HOST=

# RUST_BACKTRACE=1
# RUST_BACKTRACE=full

# in case you need to use a different compiler
# CC=clang
